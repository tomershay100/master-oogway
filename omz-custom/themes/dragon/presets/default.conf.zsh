# dragon preset: default
# Balanced. username@hostname:dir ❯, git status, time & timer.
# Plain ASCII (no Nerd Font) and a persistent (non-transient) prompt.
export DRAGON__USE_NERD_FONT='false'
export DRAGON__ENABLE_TRANSIENT_PROMPT='false'
