# mo-apps

Short aliases for GUI applications installed via flatpak. Each alias is silently skipped if flatpak is not installed.

**Dependencies:** `flatpak` — skipped silently if not installed.

| Command | Description |
|---------|-------------|
| `gnucash` | launch GnuCash (personal finance) via flatpak |
