
* `master-oogway:managed`: what would happen if someone deletes this line in zshrc?

* `export BAT_THEME='Coldark-Dark'` - so the mo-bat plugin aliases doesnt need to specify for that - easy change for everyone in zshrc