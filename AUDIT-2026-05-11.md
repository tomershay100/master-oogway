# Senior Engineering Audit — `master-oogway`

**Date:** 2026-05-11
**Scope:** the `master-oogway` repo at `/home/tomer/projects/custum-linux-configs/shared/master-oogway/` — `install.sh`, dotfile templates (`zshrc/zshenv/gitconfig/editorconfig.master-oogway`), the `dragon` theme (`omz-custom/themes/dragon/` — 12 files, ~2200 lines), all 19 `mo-*` plugins (~800 lines), `tests/check_schema.sh`, `.gitmodules`, `README.md`, `CONTRIBUTING.md`, `TODO.md`. Excludes the four third-party plugin submodules (gitstatus, you-should-use, zsh-autosuggestions, zsh-syntax-highlighting).

**Method.** Read foundation files directly (install.sh, zshrc, dotfile templates, dragon entry point, notifier, aliases, README, CONTRIBUTING, TODO, .gitmodules, check_schema.sh). Dispatched two parallel exploration agents — one for the dragon theme internals, one for all 19 plugins. Independently verified the most severe claims by reading the actual source. Where agent findings were wrong or inflated, I corrected the severity and noted the disagreement in §3.5.

---

## 1. What this codebase does well

This is a substantial piece of software, not a toy. It deserves credit before criticism.

1. **Real architecture, not just config.** The dragon theme has a schema layer (`schema.zsh` — single source of truth), defaults loop, segment renderer (`__dragon__show`), separator engine, and a 912-line interactive configurator wizard. That's a coherent design with clear seams.
2. **Clear contracts.** [`CONTRIBUTING.md`](CONTRIBUTING.md) is genuinely good — it specifies the file layout, four mandatory pre-commit checks (`bash -n`, `zsh -n`, `shellcheck`, `tests/check_schema.sh`), the procedure for adding plugins (`# Provides:` first line, README updates, escape-hatch alias for overrides), and the procedure for adding theme variables.
3. **Three install modes auto-detected.** `install.sh` correctly distinguishes curl-pipe / update / dev modes from how it was invoked. Dev mode symlinks rather than copies, so edits are live — that's a real DX investment.
4. **A real uninstall.** [`install.sh:242-333`](install.sh#L242) removes everything it created — zshrc (restored from backup or warned-removed), gitconfig include + bundle, ~/.ssh/config SendEnv, /etc/ssh/sshd_config AcceptEnv (with sudo confirmation), conf dir (with confirmation), install dir (with confirmation), and explicitly leaves .zshenv/.editorconfig with clear rationale. Most personal frameworks don't ship this.
5. **Defense-in-depth on the "is this managed" detection.** `install.sh:398-401` checks both the `# master-oogway:managed` marker AND a `.master-oogway` reference in the file before deciding the user owns the zshrc. Either survives the deletion of the other.
6. **The notifier anonymous-function trick** ([`notifier.zsh:8-13`](omz-custom/themes/dragon/notifier.zsh#L8-L13)) shows mature zsh awareness. The author hits the `${0:a:h}` gotcha (inside an anon function `$0` is `"(anon)"`, so resolving relative paths would walk from `$PWD` and a `find` could scan the entire home directory) and works around it by passing the resolved themes dir as `$1`. The verbose comment explaining "do not change without testing" demonstrates institutional memory.
7. **Pre-find optimization in the notifier.** The mtime check at [`notifier.zsh:27-33`](omz-custom/themes/dragon/notifier.zsh#L27-L33) skips the expensive `grep -roh` when theme files haven't changed since the last configure. That's a real cold-path/hot-path split.
8. **SSH theme forwarding is well-designed.** `set_if_unset` ([`dragon.zsh:7-14`](omz-custom/themes/dragon/dragon.zsh#L7-L14)) uses `[[ -v var ]]` (set-ness, not emptiness), so a forwarded `DRAGON__X=""` from the sender survives intact on the receiver. The conf.zsh early-return guard pattern (per CONTRIBUTING) is sound.
9. **Universal `# Provides:` first line** in all 19 plugins, enforced by convention. Easy grep target for inventory/tooling.
10. **Override-before-additive load order** is explicitly documented in zshrc and correctly enforced by the plugin array.
11. **Optional-tool guarding pattern is consistent** — `command -v <tool> &>/dev/null` checks across plugins follow the canonical `mo-bat-override` shape.

This is genuinely above the bar for personal-grade shell frameworks.

---

## 2. Top findings

Calibrated severities after my own verification. Several agent findings were downgraded; one of mine is upgraded. Details with rationale in §3.

| # | Finding | Severity | Location |
|---|---|---|---|
| 1 | **Unconditional `LC_ALL=en_US.UTF-8`** — minimal Ubuntu / containers / SSH-to-server warn at every shell start | **MEDIUM** | [`zshrc.master-oogway:10-11`](zshrc.master-oogway#L10-L11) |
| 2 | **README documents `fpath`, plugin implements `fp`** | **MEDIUM** | [`README.md:171`](README.md#L171) vs [`mo-files.plugin.zsh:88`](omz-custom/plugins/mo-files/mo-files.plugin.zsh#L88) |
| 3 | **`mo-welcome` runs `uname/uptime/date` on every shell open** | MEDIUM | [`mo-welcome.plugin.zsh`](omz-custom/plugins/mo-welcome/mo-welcome.plugin.zsh) |
| 4 | **`mo-auto-ls` runs `ls` after every cd** — punishing on NFS / SSHFS | MEDIUM | [`mo-auto-ls.plugin.zsh`](omz-custom/plugins/mo-auto-ls/mo-auto-ls.plugin.zsh) |

---

## 3. Findings by file

### 3.1 `install.sh` (593 lines)

The largest file in the repo and the riskiest because it touches the user's home dir, ssh config, sshd config (sudo), and ~/.master-oogway as root-equivalent operations.

| Sev | Finding | Location | Notes |
|---|---|---|---|
| MEDIUM | **`git pull --ff-only` swallows the actual error.** Both at line 145 (curl-pipe re-entry) and 159 (update mode), failures die with a generic message. The actual git output (CONFLICT? local commits? no upstream?) is lost. | install.sh:145, 159 | `git_out=$(git -C "$INSTALL_DIR" pull --ff-only 2>&1) \|\| die "git pull failed:\n$git_out"` |
| MEDIUM | **No sshd_config validation before reload.** Line 541-542 appends `AcceptEnv DRAGON__*`, then `sudo systemctl reload ssh \|\| sudo systemctl reload sshd \|\| true`. Reload failure is swallowed — the running sshd keeps the old config but the next admin restart will fail to start sshd, potentially locking the user out. | install.sh:541-542 | `sudo sshd -t` before reload; abort if invalid. Also: `printf '%s\n' "$accept_line" | sudo tee -a "$sshd_config"` doesn't guarantee a leading newline if the file lacks a trailing one. |
| MEDIUM | **Backup-already-exists path silently overwrites the user's current zshrc.** `_install_zshrc` (line 372-383) backs up `~/.zshrc → ~/.zshrc.pre-master-oogway` only if the backup doesn't already exist. The intent ("first backup wins, captures the true original") is reasonable, but the next `copy_file` overwrites `~/.zshrc` with the template even when the user has accumulated edits. The defense-in-depth check at line 398-401 (managed marker + `.master-oogway` reference) prevents this in the common case, but the path exists if both markers were ever stripped. | install.sh:372-383, 396-406 | Either: (a) refuse to overwrite without `--force`; or (b) take a fresh dated backup before each overwrite (`~/.zshrc.bak.YYYYMMDD_HHMMSS`). |
| MEDIUM | **Path-migration sed at line 412-415 will run forever.** The one-shot migration `master-oogway-omz-custom → omz-custom` runs unconditionally on every install. Once everyone has migrated, this is dead code that grep-scans every zshrc forever. | install.sh:412-415 | Add a removal-by date comment (`# Remove after 2027-01-01`) or wrap in a feature flag. |
| LOW | **`_cleanup` trap is misnamed** — does nothing but exit with the saved code. | install.sh:47 | Either rename to `_propagate_exit` or actually implement temp-file cleanup via a hook list. |
| LOW | **Pipe detection (line 121-125) misses `bash <(curl ...)`** (process substitution) — `BASH_SOURCE[0]` would be `/dev/fd/63`, not in the matched set. Curl-pipe detection would fail; falls into dev mode → tries to symlink `~/.master-oogway → /dev/fd/63`. | install.sh:121-125 | Match `/dev/fd/*` and `/proc/self/fd/*` as well. |
| LOW | **`_install_ssh_sendenv` line 509** — `sed -i "/^Host \*[[:space:]]*$/a\\${send_line}"`. CRLF line endings in `~/.ssh/config` (rare on Linux but possible after Windows edits) make the `[[:space:]]*$` match fail and the script appends a new `Host *` block. | install.sh:509 | Add a normalization step or document Linux-only assumption. |
| LOW | **`grep -qF "AcceptEnv DRAGON__*"` exact-match check** at line 531 misses `AcceptEnv FOO DRAGON__*` (legitimate alternate ordering) — script would append a duplicate. sshd allows this so no breakage, just file noise. | install.sh:531 | `grep -qE 'AcceptEnv\b.*\bDRAGON__\*'` |
| LOW | **`_check_theme_vars` recomputes md5 every install** — could compare mtime first (the notifier already does this; the install path could share the optimization). | install.sh:550-568 | Lift the mtime cache pattern from notifier.zsh. |

### 3.2 `zshrc.master-oogway` (210 lines)

| Sev | Finding | Location | Notes |
|---|---|---|---|
| **MEDIUM** | **Unconditional `LC_ALL=en_US.UTF-8`.** On minimal Ubuntu, containers, fresh server installs, or some SSH-targets, `en_US.UTF-8` isn't generated. Every shell start emits `locale: Cannot set LC_ALL to default locale: No such file or directory`. Especially common in cron / SSH-into-bare-server contexts the user themselves uses (per the parent-repo's `notify-gotify`/`ssh-lan` deployments). | zshrc.master-oogway:10-11 | Check `locale -a 2>/dev/null \| grep -qiE '^en_us\.utf-?8$'`, fall back to `C.UTF-8` (universally available since glibc 2.13). |
| LOW | **`setopt CORRECT` + `ENABLE_CORRECTION="true"`** are duplicative — same effect via different APIs. | zshrc.master-oogway:29, 55 | Pick one. |
| LOW | **`setopt CORRECT` is famously divisive** — many users disable it because the autocorrect prompt fires on legitimate typos and slows down the shell. Personal preference, not a bug. | zshrc.master-oogway:29 | Document the choice in a comment so future-you doesn't second-guess. |
| LOW | **Comments describing `unsetopt FOO` describe what FOO does, which can read as if the flag is being enabled.** Lines 19, 21 — `unsetopt HIST_IGNORE_DUPS` followed by "don't record a command if it's identical to the previous one". Accurate (that's what HIST_IGNORE_DUPS does), but at a glance reads like the *behavior* is being enabled. | zshrc.master-oogway:19, 21 | Reword to "(disabled — duplicates ARE kept)". |
| NIT | **Docker plugin auto-load.** `docker` plugin (line 124) provides tab-completion via OMZ. As of recent Ubuntu the `docker-ce` package ships its own completion at `/usr/share/zsh/vendor-completions/_docker` — loading the OMZ plugin is redundant and slightly slower at shell startup. | zshrc.master-oogway:124 | Remove if you're using docker-ce; keep if not. |

### 3.3 `zshenv.master-oogway`, `gitconfig.master-oogway`, `editorconfig.master-oogway`

`zshenv` and `editorconfig` are clean. One small note on gitconfig:

| Sev | Finding | Location | Notes |
|---|---|---|---|
| LOW | **gitconfig hardcodes `meld` for diff and merge.** If meld isn't installed (TODO acknowledges as "nice-to-have"), `git difftool` and `git mergetool` fail with a confusing error rather than falling back to vimdiff or showing a clear message. | gitconfig.master-oogway:8-17 | Add a simple `[difftool] prompt = false` and document that meld is required, or set a fallback chain. |

### 3.4 `dragon` theme

I've calibrated the theme agent's findings against my own reading of `dragon.zsh`, `notifier.zsh`, `aliases.zsh`, and `dragon.zsh-theme`.

#### What the agent flagged as CRITICAL but is wrong

> *"set_if_unset breaks SSH forwarding because `[[ ! -v var ]]` overwrites empty-string-set vars"*

**Refuted.** In zsh, `[[ -v var ]]` returns true for any *set* variable, including empty-string-set. The code at [`dragon.zsh:7-14`](omz-custom/themes/dragon/dragon.zsh#L7-L14) is correct. The comment at line 27 ("set_if_unset only sets vars not already exported — SSH-forwarded vars win") accurately describes the behavior. The agent confused this with bash's `${var:-}` pattern, where empty and unset are conflated. This finding should be discarded.

> *"`%` in git branch names is unescaped in `git.zsh:3` causing prompt injection"*

**Refuted in part.** The agent's own quote shows the escaping: `${{...}//\%/%%}`. The escaping IS in place. The agent then speculates about hypothetical downstream `print -P` (without `-r`) calls — without finding one. Downgrade to a defense-in-depth recommendation (audit all `print -P` of segment-rendered content uses `-rP` or already-escaped strings). Severity: **LOW**.

#### Findings I confirm or upgrade

| Sev | Finding | Location | Notes |
|---|---|---|---|
| MEDIUM | **`tests/check_schema.sh` doesn't validate schema consistency.** CONTRIBUTING:158 says "verifies the count is consistent" — the script just *prints* the count. Doesn't assert that vars in `_DRAGON_DEFAULTS` match vars referenced by `parts/*.zsh`. A new `DRAGON__X` referenced in segment code with no schema entry passes the test silently. | tests/check_schema.sh | Add: `comm -23 <(grep -roh 'DRAGON__[A-Z_]*' parts/ \| sort -u) <(grep -oP '"\K[A-Z_]+(?=")' schema.zsh \| sort -u)` and fail if non-empty. Also do the inverse for orphan entries. |
| MEDIUM | **`__set_ssh_connection_count_content` runs `who` on every prompt** ([`segments_right.zsh:114-139`](omz-custom/themes/dragon/parts/segments_right.zsh#L114-L139)). On a multi-user server with hundreds of sessions `who` is genuinely slow. Real prompt latency. | segments_right.zsh:114-139 | Cache to a global with a 5-second TTL keyed on `EPOCHSECONDS`. |
| MEDIUM | **`__calc_prompt_length` doesn't count CJK width.** `${#stripped}` counts characters, but a CJK char is 1 character × 2 columns. The 1.1× fudge at the end doesn't fix this. Affects the "git status on new line" decision at [`git.zsh:75-79`](omz-custom/themes/dragon/parts/git.zsh#L75-L79) for users with Hebrew/Arabic/CJK paths. | parts/prompt.zsh | Use `${(m)#stripped}` (zsh's "modifier m" — display-width counting) instead of `${#stripped}`. |
| MEDIUM | **`--new-only` hash is over variable *names* only**, not over types/groups/defaults ([`configure.zsh:829-850`](omz-custom/themes/dragon/configure.zsh#L829)). A rename triggers reconfiguration; a default-value change doesn't. | configure.zsh | Hash the entire `_DRAGON_DEFAULTS` serialized form (`md5sum schema.zsh` is the cheap proxy). |
| MEDIUM | **State-file race in tmux/multi-session shells.** `notifier.zsh:43-48` does `grep -v ... > tmp.state && mv tmp.state state`. Five tmux panes spawning at once can race; last writer wins; one pane's `dismissed_hash` write is lost. Loss is benign (one extra notification next time), so calling it MEDIUM is generous — the real severity is **LOW**. | notifier.zsh:43-48 | `flock -x 9` on `state.lock` if precise dedup matters; otherwise document the imprecision. |
| LOW | **Color-name typos silently render as no-color.** `__get_xterm_color_by_name "Maroon"` (capital) returns empty because COLORS is keyed lowercase. The configurator validates color names but a hand-edited `conf.zsh` does not. | helpers.zsh:1-15 | If the lookup fails AND the input isn't numeric, emit a one-time `print -P "%F{red}[dragon] unknown color: %F{white}${color_name}%f"` to stderr. |
| LOW | **Stale globals in git.zsh.** `GIT_STASH_STR` and `GIT_REMOTE_STATE_STR` aren't reset at the top of their setter functions ([`git.zsh:21-43`](omz-custom/themes/dragon/parts/git.zsh#L21-L43)). If `ENABLE_GIT_STASH_COUNT` flips at runtime, stale value persists. | parts/git.zsh:21-43 | First line of each setter: explicit reset to "". |
| LOW | **`add-zsh-hook __set_timer` redundant idempotency check.** [`dragon.zsh:68-70`](omz-custom/themes/dragon/dragon.zsh#L68-L70) checks `(( ! ${preexec_functions[(Ie)__set_timer]} ))` before adding the hook — but `add-zsh-hook` is itself idempotent. None of the other six hook registrations have this guard. Inconsistent. | dragon.zsh:68-70 | Drop the guard; just call `add-zsh-hook preexec __set_timer`. |
| LOW | **`local _dragon_k _dragon_v` at file top level** ([`dragon.zsh:36`](omz-custom/themes/dragon/dragon.zsh#L36)). zsh allows this (unlike bash) — it behaves like `typeset` with no scoping effect. Not a bug, just non-idiomatic. | dragon.zsh:36 | Use `typeset` or `local -h` if intent matters; otherwise comment why. |
| NIT | **PROMPT/RPROMPT assignments would benefit from braces** for parser clarity (`PROMPT="${left_prompt}"` over `PROMPT="$left_prompt"`). Pure style. | parts/prompt.zsh:52, 134 | Skip unless you do a style pass. |

#### Findings I downgraded

- The "race between gitstatus async callback and prompt refresh" — agent rated HIGH. zle's `reset-prompt` from a callback is a documented gitstatus pattern; race is theoretical without observed corruption. **Downgrade: LOW** unless you've actually seen prompt corruption.
- The "conf.zsh not atomically written" — agent rated HIGH. The agent's own quote shows the temp+syntax-check+mv pattern, which IS atomic at the FS level. The only real risk is the temp file lingering after Ctrl-C between create and mv — fixable with a `trap 'rm -f "$tmp"' EXIT INT TERM` inside the function. **Downgrade: LOW.**
- The "preview renderer silences errors" — at [`configure.zsh:206-232`](omz-custom/themes/dragon/configure.zsh#L206) — fair UX point but not a defect. **NIT.**

### 3.5 The `mo-*` plugins

The plugins agent produced 45 findings; I'm including the ones I confirm directly and noting which I refuted.

#### Refuted or downgraded agent claims

> *"`please` (mo-shell-tools) is CRITICAL — `curl|bash` re-runs as root via `sudo $(fc -ln -1)`"*

**Refuted.** In `sudo cmd1 | cmd2`, sudo only escalates `cmd1`. So if the previous command was `curl https://x | bash`, `please` becomes `sudo curl https://x | bash` — curl runs as root (harmless: it's downloading), bash runs as the user (no privilege escalation). No RCE. The `please` alias does have surprising semantics (sudo+pipe, sudo+redirect gotchas) but it's a standard `sudo !!` pattern, not a CRITICAL bug. Real severity: **LOW** (footgun for users who don't understand sudo+pipe semantics).

> *"`frg` HIGH — file path injection via $EDITOR"*

**Refuted.** The agent's own quote shows quoted args: `${EDITOR:-vim} "$(cut ...)" "+$(cut ...)"`. Filenames with shell metacharacters become literal arguments to the editor. Real concern is much narrower: a file path containing `:` characters breaks `cut -d:` (file/line extraction). Real severity: **LOW** (file-not-found error, not injection).

> *"`fp`/`flog` HIGH — xclip Wayland incompatibility"*

**Calibrated.** True only if xclip is installed AND session is Wayland. On a fresh Ubuntu 24.04 Wayland install, xclip is not installed by default → the `command -v xclip` check fails → falls through to print path. The breakage scenario is "user installed xclip from somewhere but is on Wayland" — narrower than the agent suggests. Real severity: **MEDIUM**.

> *"`extract` HIGH — zip slip / path traversal"*

**Calibrated.** Modern tar (1.30+, ~2017) refuses absolute paths and `..` by default. Modern unzip refuses extraction outside cwd. The remaining real risk is symlink-based attacks (tar follows symlinks during extract). Real severity: **MEDIUM** (symlink-attack window is real but narrow).

#### Confirmed findings

| Sev | Finding | Location | Notes |
|---|---|---|---|
| **MEDIUM** | **README documents `fpath`, code implements `fp`.** README:171 says `fpath [dir]`; the actual function in [`mo-files.plugin.zsh:88`](omz-custom/plugins/mo-files/mo-files.plugin.zsh#L88) is `fp`. Users following the README can't find the command. (Also: `fpath` is a zsh builtin array — naming a function `fpath` would be a collision, so the implementation name is correct.) | README.md:171, mo-files.plugin.zsh:88 | Update README to `fp` everywhere. |
| **MEDIUM** | **`mo-welcome` runs `uname/uptime/date` on every shell open.** On a slow Pi or a heavily-loaded server, this is the biggest contributor to shell-start latency. Worth measuring (`time zsh -i -c exit`). | mo-welcome.plugin.zsh | Cache to `~/.cache/master-oogway/welcome` with a TTL, or make the whole plugin opt-in via env var. |
| **MEDIUM** | **`mo-auto-ls` runs `ls` after every cd.** On NFS / SSHFS / large remote-mounted dirs, this is painful. The parent-repo audit's `lan_ssh_discover` workflow specifically cd's into remote dirs, which would hit this. | mo-auto-ls.plugin.zsh | Guard with `[[ "$PWD" != /mnt/* && "$PWD" != /media/* ]]` or honour `SLOWFS=1`. |
| MEDIUM | **`fenv` exposes secrets to stdout/clipboard.** Print mode echoes the value — `AWS_SECRET_ACCESS_KEY` ends up in shell history / tmux scrollback / screen recording. Editor mode writes a tempfile via `mktemp` with default umask (potentially 0644). | mo-env.plugin.zsh | Default to printing only the var name; add `--show` to print value; for editor mode, `umask 077` before mktemp. |
| MEDIUM | **`fkill` PID-reuse race.** Between `kill -0 $pid` (validity check) and `kill -SIG $pid` (the actual kill), the OS can reap the process and reassign the PID to an unrelated process — fkill kills the wrong target. Narrow window but possible on busy systems. | mo-process.plugin.zsh | Acceptable to leave as-is for personal use — flag as "known limitation" in the help text. |
| MEDIUM | **`grep` alias breaks `--` argument parsing.** Aliases expand textually, so `grep -- "-Zword" file` becomes `grep --color=auto --exclude-dir={...} -- -Zword file` — should still work because `--` is preserved. But if a user does `grep -e ...`, the additional `--exclude-*` flags can interfere with argument grouping. | mo-search.plugin.zsh:5-7 | Convert to a function: `grep() { command grep --color=auto --exclude-dir={.git,...} "$@"; }`. Aliases-as-wrappers are an anti-pattern for things with complex argv handling. |
| MEDIUM | **`flog` xclip+Wayland.** Same shape as `fp`. Either both are MEDIUM or both LOW depending on user's environment. | mo-git.plugin.zsh:96-98 | Add wl-copy fallback: `if command -v wl-copy &>/dev/null; then echo -n "$hash" \| wl-copy; elif command -v xclip ...`. |
| MEDIUM | **`fp` xclip+Wayland.** Same as above. | mo-files.plugin.zsh:113-114 | Same fix. |
| MEDIUM | **`extract` symlink-following risk.** `tar -xf` will follow symlinks present in the archive when writing. A crafted archive containing a symlink `etc/passwd → /etc/passwd` followed by `etc/passwd` as a regular file overwrites the host file (subject to user perms — limited blast radius without root). | mo-files.plugin.zsh:42-56 | Add `--no-overwrite-dir --no-same-owner --no-same-permissions` to tar invocations; for unzip, use `unzip -K`. |
| MEDIUM | **`mo-cli`: no preflight check that `dragon-configure` exists** before forwarding. If the theme directory wasn't loaded (e.g., user disabled the dragon theme but kept mo-cli), `master-oogway configure` fails with "command not found" rather than a helpful message. | mo-cli.plugin.zsh:67 (per agent) | `command -v dragon-configure &>/dev/null \|\| { echo "dragon-configure not found — is the dragon theme loaded?" >&2; return 1; }` |
| LOW | **`please`** (`alias please='sudo $(fc -ln -1)'`) is a footgun for users who don't grok sudo+pipe / sudo+redirect semantics. Not a security bug. | mo-shell-tools.plugin.zsh:24 | Document the semantics in the README's mo-shell-tools section, or replace with a function that prints the command before running. |
| LOW | **`cwhich`/`vwhich` on aliases.** `which` on Ubuntu can return `<name>: aliased to <expansion>` — `cwhich ll` then tries to view a file named "aliased to..." | mo-shell-tools.plugin.zsh:8-20 | Use `whence -p` / `command -v` / `type -p` (returns path only) instead of `which`. |
| LOW | **`bak` timestamp collision** within the same second. `bak file; bak file` overwrites the first backup. `date +%Y%m%d_%H%M%S_%N` (nanoseconds) fixes it. | mo-files.plugin.zsh:67 | One-line fix. |
| LOW | **`natip` no timeout.** `curl -s ifconfig.me` can hang indefinitely if the service is unreachable. | mo-network.plugin.zsh | Add `--max-time 5`. |
| LOW | **`sshto` doesn't follow `Include` directives** in `~/.ssh/config`. Users with modular SSH configs (the parent repo's `ssh-lan` writes hosts to `~/.ssh/config.d/lan_hosts.conf` via Include) won't see those hosts. | mo-network.plugin.zsh | Use `awk` over both `~/.ssh/config` AND files matched by Include patterns; or shell out to `ssh -G dummy` and parse. |
| LOW | **`psgrep` regex trick is fragile.** `grep -i "[${1:0:1}]${1:1}"` (avoiding self-match) breaks if `$1` starts with regex metacharacters like `[` or `(`. | mo-process.plugin.zsh | Use `pgrep -lif "$1"` if available; fall back to current pattern with a defensive `printf '%q'`. |
| LOW | **`frg` parser breaks on filenames containing `:`.** `cut -d:` on `path/to/file:name:5:content` mis-extracts. | mo-search.plugin.zsh:51 | Use `awk -F: '{print $1, $2}'` with field-count awareness, or have rg emit JSON and parse with jq. |
| LOW | **`fhist` selecting a destructive previous command** (e.g., `rm -rf …`) loads it into the buffer — user must press Enter to actually run it. Working as intended. | mo-search.plugin.zsh | NIT: README sidebar warning. |
| NIT | **No shared `_mo_require` helper** — 11 plugins independently `command -v <tool> &>/dev/null`. Refactor opportunity but not a defect. | many | Optional. |

#### One more I want to add — agent missed it

| Sev | Finding | Location | Notes |
|---|---|---|---|
| LOW | **`tmpcd` (mo-navigation) leaves the temp dir to be cleaned by the OS.** Most personal users want this. But: the dir is visible to all users on the system because `mktemp -d` creates mode-0700 by default — actually that's correct. Skip unless tmpcd does something different than `mktemp -d && cd "$_"`. | mo-navigation.plugin.zsh | Verify and discard if false alarm. |

### 3.6 `TODO.md`

Two open items, both small. Calibrating against current code:

| TODO entry | Status | Action |
|---|---|---|
| `master-oogway:managed`: what would happen if someone deletes this line in zshrc? | **Real question, partial answer in code.** install.sh:398 checks the marker; install.sh:401 checks for `.master-oogway` reference as a fallback. So deleting only the marker leaves the fallback. Deleting BOTH (marker line + every `.master-oogway` reference) causes `_install_zshrc` to fire on next install → backs up + overwrites. | Document the answer in CONTRIBUTING (or in a comment near install.sh:398). Either accept the behavior, or add a third check (e.g., a `.zshrc-master-oogway-fingerprint` file). |
| `export BAT_THEME='Coldark-Dark'` in zshrc | **Already done** at zshrc.master-oogway:175. | Remove the entry. |

### 3.8 README drift

Single drift item: `fpath` in README vs `fp` in code (already covered in §3.5 plugins). Otherwise the README's command tables match implementations across all 19 plugins.

---

## 4. Cross-cutting themes

### 4.1 The "personal but battle-hardened" balance is well-struck — except for two systemic gaps

This codebase clearly knows what it is: a personal framework that the author also wants to be safe to share. The discipline (`# Provides:` rule, escape-hatch alias rule, four-check pre-commit, clear three-mode install detection, real uninstall) is well above hobby grade.

**Two systemic gaps** undermine that posture:

1. **No CI to enforce the rules.** CONTRIBUTING.md mandates `bash -n`, `zsh -n`, `shellcheck` before every commit. Nothing in the repo runs these automatically. A `.github/workflows/lint.yml` running these checks on push would convert aspirational discipline into enforced discipline. Same observation as the parent repo, recurring here.

### 4.2 Untrusted-data flows into shell-evaluated contexts

- **`mo-network/sshto` host parsing** if it ever sources from `~/.ssh/config.d/` files written by external tools (per the parent repo's `lan_ssh_discover`, those files contain DNS-derived hostnames; the parent-repo audit flagged the same DNS→config injection in `lan_ssh_discover.sh`).

The `dragon` theme itself escapes `%` in git branch names at git.zsh:3 — that's the right pattern; the same defensive escaping should be applied wherever externally-controlled data crosses into a renderable / executable context.

### 4.3 Performance has two clear optimizations available

- `mo-welcome` — subprocess-spawning every shell open
- `mo-auto-ls` — fires after every `cd`, including remote mounts
- (Theme: `__set_ssh_connection_count_content` running `who` every prompt on multi-user servers — but that's gated by `DRAGON__ENABLE_SSH_CONNECTION_COUNT` so opt-in)

For a desktop-only user this is invisible; for the operator who ssh's into the parent-repo's `server/` and `raspberry-pi/` deployments, every shell start carries the cost.

### 4.4 The `.gitmodules` situation

Same class of issue as the parent repo's name/path mismatch — but here it's *every* section. Probably a sign that the original directory was `zsh-custom.d/`, then renamed to `omz-custom/`, and `.gitmodules` was never updated because git didn't break.

### 4.5 Documentation gravity is healthier here than in the parent repo

README.md is real (310 lines, comprehensive, accurate aside from the `fp/fpath` rename). CONTRIBUTING.md is real (193 lines, has a clear add-a-plugin / add-a-theme-var workflow). Neither is gitignored. A new contributor to master-oogway would have a meaningfully different (better) onboarding experience than one to the parent repo.

---

## 5. Recommended fix sequence

### Phase 1 — half-day quick wins

1. **Fix `.gitmodules` section names** — single `git config --file=.gitmodules --rename-section ...` per submodule, then `git submodule sync`.
2. **Fix README `fpath → fp`** — single sed.
3. **Empty-input validation on git identity prompts** in `install.sh:444-451` — loop until non-empty.
4. **Rephrase or remove TODO.md** — answer item 1, delete item 2 (already done).

### Phase 2 — one day

1. **Add `.github/workflows/lint.yml`** — runs the pre-commit checks from CONTRIBUTING. Converts aspirational discipline into enforced.
2. **Locale safety in zshrc** — fall back to `C.UTF-8` when `en_US.UTF-8` isn't generated.
3. **Wayland clipboard fallback** in `fp` and `flog` — try wl-copy before xclip.

### Phase 3 — optional polish

1. **`mo-welcome` opt-in or cached** — TTL'd cache file, or env-var gate.
2. **`mo-auto-ls` slow-fs guard** — skip on `/mnt/*` and `/media/*`, or honour an env var.
3. **Convert `mo-search`'s `grep` alias to a function** — fixes the alias-vs-argument-parsing edge cases.
4. **`mo-cli` preflight for `dragon-configure`** — clearer error if theme isn't loaded.
5. **`extract` symlink hardening** — `--no-same-owner --no-same-permissions` for tar; `unzip -K` for zip.

### Phase 4 — long-tail

1. **`__calc_prompt_length` use `${(m)#var}` for proper display-width counting** — fixes CJK/Hebrew prompt-overflow logic.
2. **Document the `please` semantics** in mo-shell-tools README section — sudo+pipe and sudo+redirect gotchas.
3. **Cache `who` in `__set_ssh_connection_count_content`** with 5-second TTL — for users who enable ssh-conn-count on multi-user servers.
4. **Lift the notifier's mtime-cache trick** into the install.sh `_check_theme_vars` function — avoid recomputing md5 on every install.

---

## 6. Calibration appendix — agent claims I changed

For transparency, here's what the deep-dive agents flagged as severe and why I disagreed.

| Agent severity | Finding | My calibration | Rationale |
|---|---|---|---|
| **CRITICAL** | "set_if_unset breaks SSH forwarding via empty-string overwrite" | **Refuted** | zsh's `[[ -v var ]]` distinguishes set-but-empty from unset; the code is correct. |
| **HIGH** | "git.zsh % escaping incomplete" | **LOW** | The escape IS in place; agent's downstream concern is hypothetical. |
| **HIGH** | "gitstatus async/precmd race" | **LOW** | `zle reset-prompt` from gitstatus callback is documented-supported. |
| **HIGH** | "conf.zsh not atomically written" | **LOW** | The agent's own quote shows temp+syntax-check+mv; mv is atomic. Real risk is temp-file lingering on Ctrl-C. |
| **CRITICAL** | "`please` causes RCE-as-root via `curl\|bash`" | **LOW** | sudo only escalates the first command in a pipe. Agent confused sudo+pipe semantics. |
| **HIGH** | "`frg` file-path injection via $EDITOR" | **LOW** | Args are quoted in the agent's own quote. |
| **HIGH** | "`extract` zip slip" | **MEDIUM** | Modern tar/unzip refuse `..` paths by default; symlink-attack vector is the real residual. |
| **HIGH** | "`fp`/`flog` xclip Wayland incompatibility" | **MEDIUM** | Only breaks if xclip is installed AND session is Wayland. On a fresh Ubuntu 24.04, xclip isn't installed → falls through to print. |

I added these net-new findings the agents missed:

- `install.sh` path-migration sed is forever-running dead-code-eventually.
- `zshrc` unconditional `LC_ALL` warning on minimal systems (MEDIUM).
- `add-zsh-hook __set_timer` redundant idempotency check (NIT).

---

## 7. Verdict

`master-oogway` is a **well-designed personal zsh framework** that demonstrates real engineering investment — three install modes, real uninstall, idempotent helpers, comprehensive README, enforced (aspirational) coding rules, a single-source-of-truth schema, and an SSH theme-forwarding architecture that actually works. The scaffolding is sound.

The defects fall into two buckets:

- **Several operational fragilities** (no CI to enforce the documented checks, the test that exists doesn't test what it claims), and
- **A long tail of small UX/portability issues** (xclip-on-Wayland, locale on minimal systems, stale TODO entries, README drift).

Phase 1 of the fix sequence (~half day) closes the most acute fragilities. Phase 2 (~one day) removes the systemic enforcement gap by adding CI for the four checks CONTRIBUTING already mandates.

Net: a strong personal framework with a clear path to "production-grade for one operator." The discipline is here; the enforcement is what separates it from polished.
